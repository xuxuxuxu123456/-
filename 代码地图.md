# 手机主题 H5 · 代码地图

> 10 个单页 HTML + 14 份 jsdom 验证脚本（8 页原件 + 主题设置 / 轮播图设置两个新页）。
> 无框架、无构建。
> 无框架、无构建，全靠 localStorage 打通页面，中文标识符（类名 / 变量名 / 存储键都是中文）。
> 本文件是外部记忆：改动前先读它，比重新翻 16000 行快得多。

---

## 一、页面速查表

| 文件 | 行数 | 职责 | 进入方式 | 出口 |
|---|---|---|---|---|
| `1_shouyeyulan.html` | 2288 | 首页：时间 / 搜索 / 三宫格配图 / 个人信息卡 / 标签 / 会话列表 | 底部导航「聊天」 | 7（点会话）、2（加号-添加好友）、4 / 5 / 8（底部导航） |
| `2_haoyouxinxi.html` | 1760 | 好友信息表单（新建 / 编辑） | 4 页「好友信息」`?id=&mode=edit`；加号 `?mode=new` | 4（保存/返回）、3（音色配置行） |
| `3_YINSEAPI.html` | 462 | 音色配置（独立页） | 2 页「音色配置」行 `?id=` | 2（返回，带 id 或 mode=new） |
| `4_tongxun.html` | 2059 | 通讯录：搜索 / 音乐播放器 / A–Z 分组 + 索引条 | 底部导航「通讯录」 | 7（发送消息）、2（好友信息）、1 / 5 / 8（底部导航） |
| `5_dongtai.html` | 1352 | 动态：3D 轮播（coverflow）+ 动态流 + 发布入口 | 底部导航「动态」 | 6（发布，带 `?type=`）、1 / 4 / 8（底部导航） |
| `6_fabudongtai.html` | 1265 | 发布动态（说说 / 图片 / 视频） | 5 页发布入口 `?type=说说\|图片\|视频` | 5（发表后回跳） |
| `7_liaotian.html` | 1890 | 聊天信息流 + 语音录制 | 1 页会话 `?id=&from=1`；4 页发消息 `?id=&from=4` | 按 `from` 回 1 / 4，缺省回 1 |
| `8_wode.html` | 813 | 我的：时段图标（昼夜 SVG）+ 拍立得照片墙 | 底部导航「我的」 | 1 / 4 / 5（底部导航） |
| `9_zhutishezhi.html` | — | ★ 主题设置：6 张内置主题 + 相册/拍照 9:16 裁剪上传，改完全站生效 | 1 页加号菜单「主题设置」 | 按 `?from=` 回 1 / 4 / 8，缺省回 1 |
| `10_lunbotu.html` | — | ★ 轮播图设置：4 张各自独立替换，16:9 裁剪 | 5 页轻点轮播卡片 `?序=0~3` | 5（返回 / 完成） |

底部导航四 tab：**聊天(1) · 通讯录(4) · 动态(5) · 我的(8)**。2 / 3 / 6 / 7 / 9 / 10 是二级页，不带底部导航。

★ 主题背景为**全局设置**：1~8 页与 10 页在 `</body>` 前各挂了一段读取脚本（读 `主题背景` 键），
  改一处即全站生效。新增页面时记得补这一段，否则新页会用写死的默认图。

---

## 二、数据契约（localStorage）

采用 **「索引表 + 按 id 分桶」** 架构：

```
联系人索引 = [{ id, 名称, 备注, 头像, 消息, 时间 }]   ← 全局唯一真相源，1 / 4 / 5 / 7 都读它
好友信息_<id>  → 完整表单对象（昵称/生日/身高/性别/身份/世界观/人物信息/性格标签/人物属性/音色）
好友头像_<id>  → 头像 dataURL
音色配置_<id>  → { 音色, 自定义ID, 音速, 语调, 语言, 性别, 已启用 }
聊天记录_<id>  → 消息数组
```

| 键 | 写方 | 读方 | 说明 |
|---|---|---|---|
| `联系人索引` | 1、2 | 1 / 4 / 5 / 6 / 7 / 8 | 缺省时用旧键 `会话列表` 迁移一次 |
| `好友信息_<id>` | 2 | 2、3、7 | 7 页从这里抽「口头禅」铺对话 |
| `好友头像_<id>` | 2 | 1 / 4 / 5 / 7 | |
| `音色配置_<id>` | 3 | 3 | 新建时用临时键 `音色配置_临时`，2 页保存时合并；兼容旧全局键 `音色配置` |
| `聊天记录_<id>` | 7 | 7 | 有存档就以存档为准，不再造初始对话 |
| `我的动态` | 6 | 5、8 | |
| `主题背景` | 9 | **1~8、10 全部** | `{ 源:'默认', 索引:0~5 }` 或 `{ 源:'自定义', 数据:dataURL }`；0=默认主题背景，1~5=背景1~背景5 |
| `轮播图_<序>` | 10 | 5 | `序 = 0~3`，dataURL；读不到时 5 页按 `3D图1~4` 做扩展名探测 |
| `当前联系人` | 4、7 | 7 | 无 `?id=` 时的兜底 |
| `用户昵称` | 1 | 5、6 | |
| `音乐播放状态` / `音乐列表` / `音乐库` | 4 | 4 | 续播 + 上传曲目存 IndexedDB |
| `已授权_<名>` | 1 / 2 / 5 / 7 | 同左 | 权限记忆，**四处前缀必须同步**（源码有「改动请四处同步」注释） |
| `已授权位置` | 5 | 5 | 缓存经纬度，避免重复定位 |

⚠️ id 形态多样：`c_home_N`（1 页导出）、`c_legacy_N`（旧数据）、`c_default_N`（内置默认）、`c_name_xxx` / `cx_xxx`（7 页按名生成）、`me_xxx`（6 页）——**档案一律按昵称匹配，不要依赖 id 形态**。

---

## 三、四个默认角色（内置档案）

档案集中在 `角色档案.js`（旧名 → 新名迁移映射也在这里）：

| 现名 | 旧名 | 属性 | 标志词 | 生日 / 身高 |
|---|---|---|---|---|
| 白九霄 | ෆ半岛茶 | **妖** | 九州 | 1050-05-31 / 172 |
| 埃洛温·影蚀 | 恋痛症 | 人类 | 艾诺拉 | 2033-08-30 / 184 |
| 陆沉渊 | 喵喵又咪咪 | 人类 | 北都·京华 | 1998-11-11 / 187 |
| 林彦 | 水色 | **兽人** | 蛮荒之境 | 2050-12-21 / 195 |

头像映射：白九霄→圆形头像5、埃洛温→2、陆沉渊→4、林彦→3（注意不是按顺序）。

---

## 四、验证套件地图

跑法：`cd /data/inputs && ./run_all.sh`（改过页面放工作区时用 `PAGES_DIR=/data/workspace ./run_all.sh`）。

公共模块 `testkit.js`：`清理()`（屏蔽图片/音乐）、`造存储()`、`造断言器()`、`起页面()`、`跑2页/跑3页/跑4页`、`音色隔离()`、`收尾()`。

| 脚本 | 守什么 | 关键回归哨兵 |
|---|---|---|
| `verify_characters.js` | 四角色档案（数据驱动，**已取代下面 4 份老脚本**） | A–K：改名、迁移、互不干扰、空壳接管、新建不污染、分桶优先、音色隔离 |
| `verify_baijiuxiao / elowen / luchenyuan / linyan.js` | 单角色老脚本，留作交叉验证 | 与上面重复，改动时互为对照 |
| `verify.js` | ID 分桶架构全流程（4→2→3→4 回写） | 13 步：含 storage 事件自动重渲染、旧 `会话列表` 迁移 |
| `verify_permissions.js` | 权限记忆（位置 / 麦克风 / 相册） | ★已授权却失败 → 必须清标记，不能装成功 |
| `verify_album_upload.js` | 相册/拍照上传 | ① `display:none` 会让移动端 click 失效→用 `.视觉隐藏` ② HEIC 拦截 ③ EXIF orientation 大小端 ④ 1600 万像素降采样 |
| `verify_crop_preview.js` | 裁剪预览可见性 | ★ `transform-origin` 必须 `0 0`，否则大图被推出视口 |
| `verify_zhuti.js` | ★ 9 / 10 页（主题设置 + 轮播图） | 6 张内置主题文件名、点选写入 `主题背景`、`.视觉隐藏`+capture 分工、EXIF 大小端、9:16 裁剪几何覆盖、1~8 页都挂了读取脚本、轻点卡片带正确 `序` |
| `verify_dongtai.js` | 5 页轮播 + 动态流 | 8 张卡（4×2 无缝）、视窗按 `--卡宽比` 反推高度、无底框、点赞持久化 |
| `verify_fabu.js` | 6 页发布 | 标题随 type 变、谁可以看三种模式、通讯录只读一次 |
| `verify_liaotian.js` | 7 页聊天（**最大，A–AL 共 38 组**） | 气泡 40% 玻璃、横版无尖角、录音全链路（静音拒绝上屏 / Blob 带 MIME / 播放器挂 DOM）、长按删除、清空后不重生 |
| `verify_wode.js` | 8 页时段图标 | 24 小时逐点验算昼夜与天体坐标、位置随分钟连续 |

⚠️ jsdom 无布局引擎：`clientWidth/offsetWidth` 恒为 0。**凡几何断言都必须先打桩尺寸**（参考 `verify_dongtai.js` 的 400×272 与 `verify_crop_preview.js` 的 300 视口）。
⚠️ jsdom 不实现导航：`location.href` 会报 `Not implemented: navigation`，统计报错时要剔除（非 bug）。
⚠️ **testkit 默认 `PAGES_DIR=/data/inputs`**：在别处跑验证若不设 `PAGES_DIR`，测的是原件而不是改动版 —— 跑之前先确认读的是哪份。

**已知历史债（改 `verify_wode.js` 前必读）**：基线就有 5 项失败 + 1 处崩溃，与页面当前实现不符：
`SVG 含 4 颗星`（实际星数已变）、06/12/18/00 点天体落点期望值过时、`[F] 资料卡` 与 `[G] 数据条` 测的是**已被精简掉**的资料卡（8 页现只剩状态栏 + 拍立得）。修脚本时这几组要按现状重写，别当成回归。

---

## 五点五、工作区约定

- **`/data/inputs` 只读**，改页面必须先复制到 `/data/workspace` 再改，跑验证用 `PAGES_DIR=/data/workspace node verify_xxx.js`。
- 沙盒**没有无头浏览器**（无 playwright / puppeteer / chromium），视觉改动无法截图验证 —— 靠几何手算（见下）+ 跑验证脚本确认零回归。
- jsdom 需先 `npm install jsdom`（沙盒可联网，1 秒装完）。

### 8 页拍立得几何速算（改尺寸时套这个）
```
区宽 = min(46vw, 172px)        相纸高 = (区宽-2-14) + 7 + 24 + 2 ≈ 189
重叠 = 14% × 区宽 ≈ 24px        相邻露出高度 ≈ 165px      整组总高 ≈ 519px
旋转后包围盒单侧膨胀（卡3 最大 7.5°）≈ 11.6px < 右侧留白 30px ✓
```
改 `width` 或 `padding` 时重叠量会跟着变（margin-top 百分比按包含块宽度算），别只调一个值。

> **已废弃**：2026-09-11 起 8 页拍立得（相纸 / 宽下巴 / 负 margin 叠放 / `.拍立得` `.照片框` 类名）**已删除**，改为一列三个 1:1 方形图（`.照片列` / `.方形图`）。上面这段几何仅作历史参考。

---

## 五、跨页统一约定（改一处要同步多处）

1. **色板**：`--ink / --ink-2 / --ink-3 / --ink-4 / --accent / --accent-soft / --panel / --line / --shade`，8 页的 `:root` 完全一致。
2. **毛玻璃**：`.玻璃`（blur 14px）与 `.玻璃-薄`（8px），`--glass: 0.5`。
3. **全局白遮罩**：主题背景图上盖 `rgba(255,255,255,0.45)`（2 / 3 页是 0.4，略有出入）。
4. **权限前缀** `已授权_`：1 / 2 / 5 / 7 四处同步。
5. **中间标题居中**：5 / 8 页用 `position:absolute; left:0; right:0; text-align:center` 脱离 flex 流；7 页用左右各留 `clamp(60px,17.5vw,76px)`。
6. **容器**：`.手机主题背景容器` max-width 430px + `100dvh` + safe-area padding。
7. **占位图**：同级 `2【图片】/`（默认主题背景、极简风格1-3、圆形头像1-5、3D图1-4），音乐在 `1【音乐】/`，都带扩展名探测 png→jpg→jpeg→webp。

---

## 六、函数级索引（改动前先在这里定位）

### 1_shouyeyulan.html
- 时间：`更新时间()`
- 搜索：`执行搜索()` + `高亮()` / `转义HTML()` / `转义正则()`（拼 innerHTML 前必须转义）
- 原地编辑：`进入编辑()` / `结束()` / `失焦()` / `按键()` / `粘贴()`（只收纯文本）/ `是否空()` / `清理()` / `保存全部标签()`
- 裁剪：视口尺寸() → `初始化状态()`（cover 初始缩放）→ `应用变换()` / `缩放到()` / `约束()`（任一边不许缩进取景框）
- 上传：`处理选中文件()` → `拦截HEIC()` → `读方向()`（只读前 128KB）→ `规范化图片()`（摆正+降采样一次解码）→ `降采样若过大()`
- 权限：`权限已授权()` / `记住权限()`
- 持久化：`读取配图()` / `写入()` / `保存结果()`
- 颜文字弹窗：`渲染()` / `打开()` / `关闭()` / `添加()` / `校正当前()`（自定义删光后回落预选）
- 加号菜单：`打开()` / `关闭()` / `定位()`（按 + 实际位置定位）
- 对外：`window.打开会话()`

### 2_haoyouxinxi.html
- 键：`详情键()` / `头像键()` / `音色键()`（★ 无 id 时必须叫 `音色配置_临时`，与 3 页一致）
- 档案：`昵称命中档案()` / `查档案()` / `是空壳()`（昵称不计入空壳判定）
- 迁移：`迁移旧数据()` / `迁移索引()` / `修正旧名()` / `查找索引项()`
- 日期：`日期键()`（统一 YYYY-MM-DD，避免时分秒干扰）/ `解析日期()`（按本地时区解析，ISO 会被当 UTC 差一天）
- 其余同 1 页：裁剪 / 上传 / 权限 / 标签
- 特例：`转为新建模式()`（edit 但找不到 id 时降级）

### 3_YINSEAPI.html（最规整，462 行）
`分桶键()` → `读取配置()`（分桶 → 全局兜底 → 默认）→ `回填表单()` → `参数变更()` → `校验()` → `执行写入()` → `刷新顶部开关()` / `切换启用()`

### 4_tongxun.html
- 索引：`迁移索引()` / `修正旧名()` / `读索引()` / `分组()` / `排序键()`（备注 ‖ 昵称，同微信）
- 拼音：`取首字母()`（内嵌 20902 字全表，按码点 O(1) 索引，零依赖）
- 索引条：`渲染索引条()` / `字母按位置()` / `定位到()` / `显示气泡()`
- 搜索：`执行搜索()` / `写回名称()`（有备注渲染成「备注（原名）」）
- 音乐：`组装()`（三首默认 → localStorage → IndexedDB 上传）/ `载入()` / `尝试续播()`（被拦就等首次手势补播）/ `切歌()` / `按位置跳转()` / `保存状态()`（显式传播放中）/ `定位已存()` / `校准列表高度()`
- 联动：`需要重渲染()`（首页跳转不派发 storage，回来要重渲染）

### 5_dongtai.html
- 天气：`取定位()`（已授权直接用缓存坐标）→ `查OpenMeteo()` → 失败 `查Wttr()` → 再失败兜底福州；`读缓存/写缓存`（10 分钟）
- 轮播：`量尺寸()` → `应用()` / `帧()`（rAF 按时间差匀速推进，只写 transform/opacity/z-index）
- 图片：`载入图片()` / `试下一个()`（扩展名探测）
- 动态流：`建卡片()` / `渲染()` / `相对时间()` / `取头像()`（头像缓存）/ `读赞`（点赞持久化）
- 对外：`window.动态流渲染()`

### 6_fabudongtai.html
`读联系人()` → `渲染媒体()` / `加入媒体()` / `选文件()` / `打开媒体面板()` → `同步发表键()` → 可见性三模式 `打开选人面板()` / `渲染选人列表()` / `读上次可见性()` / `写可见性()`（记住上次选择）→ `返回上一页()`

### 7_liaotian.html（最复杂，1890 行）
- 定位：`找联系人()`（URL id → `当前联系人` → 占位）
- 对话：`抽口头禅()` → `造初始对话()`（有存档就不造）/ `渲染()` / `渲染文()` / `时分()` / `日期文案()`
- 发送：`发送()` → `落地()` → `同步会话()`（回写会话列表）
- 表情：`开合表情()` / `渲染表情格()` / `切换分类()` / `插入文本()` / `贴合输入框()`（面板贴输入区顶边，输入框长高要重测）
- 长按：`取点()` / `开始长按()` / `取消长按()` / `绑长按()` / `请求删除()`
- 录音：`切换录音()` / `开始录音()` / `停止录音()` / `发出语音()` / `录音发送()` / `更新计时()` / `释放麦克风()`
- 音量：`起音量监测()` / `停音量监测()` / `起模拟音量()`（iOS 放弃真实监测）/ `画音量()` / `检查是否无声()`
- 播放：`起播放()` / `停播放()` / `切换播放()` / `复原进度()` / `建语音条()`（DOM 里挂单例 `<audio id="语音播放器">`，iOS 才出声）
- 存储：`打开语音库()` / `跑语音事务()` / `存语音()` / `删语音()` / `清空语音()` / `载入语音()`（IndexedDB）
- 对外诊断：`window.语音诊断()` / `是否录音中()` / `取消息表()` 等

### 8_wode.html
`弧上点(t)` / `时段名()` / `混()` / `rgb()` / `查色()` → `取天空色` / `当前时段` / `刷新状态栏` / `加载拍立得` / `试下一个`（对外挂 window，供 verify_wode 验算）
- **布局是「左侧寄语 + 右侧方形图」三行**（`.照片列` > `.照片行` > `.寄语` + `.方形图`），图片定宽靠右成一列，**不是**拍立得相纸（相框已删）。
- `window.加载拍立得` 是**历史接口名**（外页可能调用），内部改成往 `.方形图` 塞图；逻辑靠 `img.parentElement` 找框，改结构时 img 的直接父必须是框。
- ★ **白色内发光与 1 页「极简配图框」同款**：`inset 0 0 25px 6px rgba(var(--panel), 0.9)`。
  写法是**两处都写**——容器上写一份（保底，图缺失时可见）+ `::before` 覆盖层再写一份（压在 img 之上，真正看得见）。
  原因：`<img>` 是内容层，会盖住容器自身的 inset 阴影；只有 positioned 且层叠在后的覆盖层才发光得出来。
  （1 页本身只写了容器那层，所以它的内发光在有图时其实是看不见的——8 页补了覆盖层。）
  圆角 10px、img `position:absolute; inset:0` 也都与 1 页对齐。
- **紧凑布局**：`gap: clamp(8px, 2.4vw, 11px)` ≈ 图宽 6.2%，与 1 页配图框 6.3% 同档；上下留白也收了。
- **寄语区**：每行 = `.寄语`（flex:1）+ `.方形图`（定宽靠右），三行。寄语 `text-align:center`，与图片 `align-items:center` 垂直对中。
  - 双语短句各三句，**断行用 `<br>` 手工控制**（自动换行会破坏居中排版的节奏），改文案时行数变了要重算高度。
  - 字号：中文 `clamp(10px,2.85vw,11.5px)`/1.75、英文 `clamp(7.5px,2.05vw,8.5px)`/1.5、中英间距 7px → 最高一行 118px ≤ 图片 150px。
    上限仍是 150px：**调大字号或行高都可能把行撑高、破坏图片等距**，改前先按这个公式算一遍。
    字距中文 .04em（字小了笔画容易糊，比常规多给一点）。
  - **★ 寄语点击可编辑**：点中文/英文任意一处进编辑（与 1 页昵称/签名同一套 contenteditable 方案）。
    失焦保存、**Esc 撤销**、Enter 换行（多行，收工靠失焦）、粘贴只收纯文本。
    存 `innerHTML`（换行靠 `<br>`，用 textContent 会丢），键：`寄语_1_中` / `寄语_1_英` / `寄语_2_中` …… 共 6 个。
    清空 → 退回 `data-占位` 占位语（「写下中文寄语」/「Write in English」），仍可点回来重写。
    跨标签页同步：监听 `storage` 事件。
    ⚠️ 存的是 innerHTML 且会写回 DOM —— 本地单用户 H5 沿用 1 页既有约定，若将来接入外部内容需先做清洗。
- **图片无黑色落影**：`.方形图` 只保留内发光那一层，不要加 `--shade` 外阴影。
- **★ 点照片可换图（相册/拍照 → 裁剪 → 上传）**：与 1 页配图同一套方案，存储键 `我的照片`（数组，按框索引），输出 600px 方形 JPEG。
  - 三个移动端坑：HEIC 提前拦（否则「选完没反应」）、EXIF orientation 摆正（否则产物是歪的）、>1600 万像素降采样（否则内存爆）。
  - ★ **`transform-origin: 0 0` 必须有**：默认中心点会让大图被推出视口，裁剪预览一片空白。
  - ★ **上传后要摘 `.空` 类并置 `img.dataset.已换图='1'`**：`.空 img` 是 `display:none` 会挡住新图；
    而占位图的扩展名探测是**异步 error 链**，不置标记会被它把新图覆盖回占位图。
  - 权限前缀 `已授权_` 本页也有一份（记「相册」/「相机」），与 1/2/5/7 页同步。

- **★ 设置菜单（照片列下方）**：五项 —— 主题背景设置 / 我的人设 / 动态轮播图 / 文本对话API设置 / 音频API设置。
  - **跳转目标集中在脚本 `设置项配置` 数组里**，改地址只动 `页` 一个字段。
  - 已接入：我的人设→`2_haoyouxinxi.html`、动态轮播图→`5_dongtai.html`、音频API设置→`3_YINSEAPI.html`。
  - **未接入**（`页` 留空）：主题背景设置、文本对话API设置 —— 自动加 `.未接入` 标灰（opacity .62）、点了给 toast「敬请期待」（绝不静默）。**目标页做好后在这里补文件名即可**，渲染/图标/点击逻辑都不用改。
  - 条目是**单行纯文字**（只有标题），无副标题小字。
  - 箭头用 SVG 描边而非 `›` 字符（各系统字形粗细高低不一致）。
  - **无底框**：`.设置区` 只有 margin，不要背景 / 描边 / 圆角 / backdrop-filter；条目间留一条 `rgba(line,.1)` 极淡分割线。
  - **图标是镂空小圆点**：`圆点SVG` 常量（`circle r=6.4`、`fill=none`、`stroke-width=2`），尺寸 `clamp(9px,2.6vw,11px)`。配置项里不再各带一份图标。

**字体栈不引外链 webfont**（离线 H5 依赖网络字体易闪字）：中文 Ma Shan Zheng → STXingkai → 楷体；英文 Snell Roundhand → Segoe Script → Brush Script MT。
  - 整列高约 505px（150×3 + gap×2 + 上15 下19）：844 / 932 屏一屏放得下，667 小屏需下滑约 56px。
  ⚠️ 间距已是偏紧档，若嫌挤，`gap` 往 `clamp(12px, 3.5vw, 18px)` 调（≈图宽 9%）更接近呼吸感。

---

## 七、建议的阅读批次

| 批次 | 内容 | 量 |
|---|---|---|
| 1 | 架构总览 + 数据契约（本文件 + `testkit.js` + `角色档案.js`） | ~300 行 |
| 2 | 1 / 4 页：列表、索引、迁移、音乐播放器 | ~4300 行 |
| 3 | 2 / 3 页：表单、内置档案、音色分桶、裁剪上传 | ~2200 行 |
| 4 | 5 / 6 页：3D 轮播、动态流、发布 | ~2600 行 |
| 5 | 7 页：聊天 + 语音录制（最复杂，单独一批） | ~1900 行 |
| 6 | 8 页 + 验证套件 | ~4200 行 |
